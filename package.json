{
  "name": "hospital-workflow-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic workflow orchestration layer for hospital admin workflows",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=packages/backend",
    "build:backend": "npm run build --workspace=packages/backend",
    "dev:frontend": "npm run dev --workspace=packages/frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
